# EXAMPLE ONLY — do not put real secrets here.
# Create real secret via:
#   kubectl create secret generic gpa-secrets -n gpa-production \
#     --from-literal=database-url='postgresql://...' \
#     --from-literal=jwt-secret='...' --dry-run=client -o yaml | kubeseal --format yaml | kubectl apply -f -
# Or use external-secrets. This file is a placeholder for `kubectl apply --dry-run` review.
apiVersion: v1
kind: Secret
metadata:
  name: gpa-secrets
  namespace: gpa-production
type: Opaque
stringData:
  database-url: 'postgresql://gpa_admin:CHANGE_ME@postgres:5432/gpa_study_hub'
  jwt-secret: 'CHANGE_ME_TO_RANDOM_64_CHARS'
  encryption-key: 'CHANGE_ME_TO_RANDOM_32_HEX'
  admin-code: 'GTU-ADMIN-2025'
